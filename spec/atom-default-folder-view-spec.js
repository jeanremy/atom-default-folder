'use babel';

import AtomDefaultFolderView from '../lib/atom-default-folder-view';

describe('AtomDefaultFolderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
